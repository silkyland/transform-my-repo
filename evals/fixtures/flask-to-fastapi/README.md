# flask-to-fastapi

A transform-my-repo eval fixture for the **GO** verdict. A tiny Flask app to
be ported to FastAPI — same ecosystem, every dependency has a direct target
equivalent, and the only real semantic gap is sync → async handlers.

Expected behavior is in `../../scenarios.md` (Scenario 1).
